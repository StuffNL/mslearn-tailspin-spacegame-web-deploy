trigger:
- '*'

variables:
  buildConfiguration: 'Release'
  releaseBranchName: 'release'

stages:
- stage: 'Build'
  displayName: 'Build the web application'
  jobs: 
  - job: 'Build'
    displayName: 'Build job'
    pool:
      vmImage: 'ubuntu-16.04'
      demands:
        - npm
    variables:
      - group: Release
      - group: KeyVault
    steps:
    - powershell: |
        # Using an input-macro:
        Write-Host "This works: $(WebAppNameDev)"

